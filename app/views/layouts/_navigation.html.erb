<% if user_signed_in? %>
  <p class="pull-right">&nbsp;&nbsp;<%= link_to 'Logout', users_sign_out_path, :method=>'delete' %></p>
	<% if (params[:controller] == 'home' and params[:action] == 'index' ) %>
		<% unless @notifications.empty? %>
			<p id='notifications-link'><a href="#notificationModal" role="button" class="pull-right" data-toggle="modal"><%= @notifications.count %> notifications</a></p>
		<% end %>		
	<% else %>
		<p class="pull-right"><%= link_to 'Back', root_path, :'data-no-turbolink' => true %>&nbsp;&nbsp;</p>
	<% end %>
<% else %>
	<% unless (params[:controller] == 'home' and params[:action] == 'new' ) %>
		<p class="pull-right"><%= link_to 'Back', root_path, :'data-no-turbolink' => true %></p>
	<% end %>
<% end %>
<br />