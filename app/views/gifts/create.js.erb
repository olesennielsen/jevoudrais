/* Add the new comment to the top of the messages list */
$("<%= escape_javascript(render @gift) %>").hide().appendTo("#gifts").fadeIn(1000);

/* Reset the message form */
$("#gift-name").val('');	



