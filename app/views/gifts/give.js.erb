$("#give-" + <%= @gift.id %>).hide();

var giver;
$.ajax({
	type: 'GET',
	url: '/get_giver/' + <%= @gift.giver_id %>,
	success: function(data)
	{
		var name = '';
		$.each(data, function(i, item) {
			name += item;
		});
		$('#giver-' + <%= @gift.id %>).html(name);
	}
});